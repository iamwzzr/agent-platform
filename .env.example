# Host port published by the local Docker Compose frontend.
AGENT_PLATFORM_HTTP_PORT=8080

# Global Agent revision budget; zero disables revision. Allowed range: 0..5.
# This does not select OpenAI; Compose intentionally keeps the Mock Provider.
AGENT_PLATFORM_AGENT_MAX_REVISIONS=2
